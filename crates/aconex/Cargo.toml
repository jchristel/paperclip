[package]
name = "aconex"
version = "0.0.2"
edition = "2024"

# Empty for now. We add reqwest, serde, etc. in the next session when we
# write the first real client code.
[dependencies]
base64 = "0.22.1"
quick-xml = { version = "0.40.1", features = ["serialize"] }
reqwest = { version = "0.13.4", features = ["json"] }
serde = { workspace = true, features = ["derive"] }
thiserror = "2.0.18"
tokio = { version = "1.52.3", features = ["full"] }
